{
  "name": "docker-built-test",
  "version": "1.0.0",
  "description": "Test using GH Actions to build on remote docker instance",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "node-red": "^4.0.0"
  }
}
